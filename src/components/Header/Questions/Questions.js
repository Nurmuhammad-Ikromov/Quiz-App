import { useState } from 'react';

export const Questions = ({ quiz }) => {

    console.log(quiz);
    //  quiz.map((e) => e.category === "History"? setCategory([...category]) : "");

	return <>
    {
       
    }
    </>;
};
